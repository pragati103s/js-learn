const myObject = {
    js : 'javascript',
    cpp: 'c++',
    rb: 'ruby',
    swift: 'by aplle'
}

// for in loop
for (const key in myObject) {
    console.log(key, myObject[key]);
    
}

const programmin = ['js', 'cpp', 'java']
for (const key in programmin) {
    console.log(key, programmin[key]);
    
}
